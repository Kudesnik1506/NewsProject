# NewsProject — Claude Code Guide

## Проект

**NewsProject** — автоматизированный аналитический новостной агрегатор с мульти-агентной архитектурой. Специализация: геостратегия с уклоном в геопсихологию — анализ не только что происходит и кому выгодно, но и как события воздействуют на сознание элит, масс, союзников и противников.

Выходной продукт: ежедневный Markdown-дайджест по стране, разделённый на две части: верифицированные события (полный анализ по кластерам) и непроверенные сигналы (краткий список для отслеживания).

---

## Архитектура: 6-агентный пайплайн

```
User → Orchestrator
         ↓
      Collector → Filter → Verifier → Analyst → Reporter
                                                    ↓
                                            reports/{date}_{country}.md
```

Каждый агент — отдельный вызов Claude API. Данные между агентами передаются как JSON. Оркестратор координирует через `tool_use`.

### Агенты и файлы

| Агент | Файл промпта | Python-реализация | Роль |
|-------|-------------|-------------------|------|
| Orchestrator | `agents/orchestrator.md` | `main.py` | Координатор пайплайна |
| Collector | `agents/collector.md` | `agents/collector.py` | Сбор сырых статей |
| Filter | `agents/filter_agent.md` | `agents/filter_agent.py` | Дедупликация, оценка релевантности |
| Verifier | `agents/verifier.md` | `agents/verifier.py` | Кросс-верификация источников |
| Analyst | `agents/analyst.md` | `agents/analyst.py` | Геостратегический + геопсихологический анализ |
| Reporter | `agents/reporter.md` | `agents/reporter.py` | Финальная сборка дайджеста: VERIFIED по кластерам + UNVERIFIED радар |

---

## Модели данных (`models.py`)

```python
Article          # Одна статья: title, url, source, content, published_at, country,
                 # relevance_score, verification_status, source_count, verification_note

Scenario         # description + probability (высокая / средняя / низкая)

AnalyzedStory    # article + what_happened + interests + power_shift +
                 # psychological_dimension + scenarios + cluster

Report           # date + country + stories + total_sources + verified_count + file_path
```

**Важно:** `AnalyzedStory` должен содержать поле `psychological_dimension: str` — добавлено в промпт analyst.md, но может отсутствовать в `models.py`. При работе с моделями — проверить и добавить.

---

## Ключевые концепции

### Редакционная формула (5 вопросов Analyst)
1. Что случилось? (факты: кто, что, когда, где)
2. Какие интересы за этим стоят? (игроки, мотивации)
3. Как это меняет баланс сил? (кто усиливается / теряет)
4. **Психологическое измерение** — механизм воздействия, целевая аудитория, нарративы, когнитивные искажения
5. Какие сценарии наиболее вероятны? (2–3 сценария с вероятностью)

### Статусы верификации
| Статус | Условие |
|--------|---------|
| `VERIFIED` | 3+ независимых источника, нет противоречий |
| `UNVERIFIED` | 1–2 источника |
| `DISPUTED` | Источники противоречат друг другу в фактах |

### Шкала relevance_score (0–10)
| Оценка | Значение |
|--------|----------|
| 9–10 | Критически важное: влияет на глобальный баланс сил или запускает масштабную психологическую операцию |
| 7–8 | Значимое: явные международные или геопсихологические последствия |
| 5–6 | Интересно в контексте: раскрывает паттерн поведения или нарратив |
| < 5 | Вне охвата — отсеивается фильтром |

### Тематические кластеры
`геополитика` / `торговля` / `военное` / `дипломатия` / `экономика` / `геопсихология`

---

## Источники данных

- **RSS**: Reuters, AP, NYT, WaPo, Politico, Bloomberg (конфиг в `config.py`)
- **NewsAPI**: `newsapi-python`, фильтр по стране
- **Веб-поиск**: Tavily или Brave Search API
- **Telegram**: `telethon` (опционально, требует ручной авторизации)

Добавить страну = добавить блок в `config.py`, без изменения кода агентов.

---

## Переменные окружения (`.env`)

```
ANTHROPIC_API_KEY=
NEWSAPI_KEY=
TAVILY_API_KEY=
TELEGRAM_API_ID=        # опционально
TELEGRAM_API_HASH=      # опционально
TELEGRAM_PHONE=         # опционально
```

---

## Стек

```
anthropic>=0.40.0       # Claude API + Agent SDK (tool_use)
pydantic>=2.0.0         # модели данных
feedparser>=6.0.0       # RSS
newsapi-python>=0.2.7   # NewsAPI
tavily-python>=0.3.0    # веб-поиск
httpx>=0.27.0           # HTTP-клиент
python-dotenv>=1.0.0    # .env
telethon>=1.36.0        # Telegram (опционально)
```

Модель: `claude-sonnet-4-6`

---

## Статус разработки

- [x] Архитектура спроектирована
- [x] Промпты всех 6 агентов написаны (`agents/*.md`)
- [x] Весь Python-код реализован (`python/`)
- [x] Зависимости установлены (`.venv/`)

## Запуск

```bash
run.bat --country USA --fast          # быстрый (без верификации)
run.bat --country USA                 # полный пайплайн
run.bat --country USA --topic "тема"  # с тематическим фокусом
```

---

## Соглашения

- Агенты не фильтруют и не интерпретируют данные вне своей роли — строгое разделение ответственности
- Данные не изменяются при передаче между агентами
- Пайплайн не останавливается из-за сбоя одного агента — логировать, продолжать
- Язык дайджеста: русский
- Формат выходного файла: `reports/YYYY-MM-DD_{COUNTRY}.md`
